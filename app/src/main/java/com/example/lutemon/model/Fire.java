package com.example.lutemon.model;

public class Fire extends Lutemon {
    public Fire(String name, int attackDice, int attackCount, int maxHealth, String imageResource) {
        super(name, "Fire", attackDice, attackCount, maxHealth, imageResource, "element_fire");
    }
}
